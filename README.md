# URL Shortener

This is a simple Node.js project demonstrating a URL Shortener.

## Endpoints

- `POST /shorten`
- `GET /:code`
- `GET /stats/:code`

## Deployment

This project is configured for deployment on Vercel.
